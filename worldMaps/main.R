## comment these in or out as you wish/need

source('prelims.R')  # install packages (commented out) and load libraries
source('datainfo.R') # prints URLs required for basemap script
source('basemap.R')  ## [ these two scripts could be combined
source('moremap.R')  ## [ i.e. skip 'save as .png' step and carry straight on to further map layers
